# ── 依赖 ───────────────────────────────────────────
node_modules/

# ── Next.js 构建产物 ────────────────────────────────
.next/
out/

# ── 环境变量（禁止提交真实密钥，规范 4-4） ────────────
.env
.env.local
.env.*.local
# .env.example 允许提交（不含真实密钥）

# ── Prisma ─────────────────────────────────────────
# 生成的 Client 不提交（构建时自动生成）
prisma/generated/

# ── 日志 ───────────────────────────────────────────
*.log
logs/

# ── 系统文件 ────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE / 编辑器 ────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── TypeScript 增量构建缓存 ─────────────────────────
tsconfig.tsbuildinfo

# ── 杂项 ───────────────────────────────────────────
*.pem
.vercel
